# Producción (Render). Para API local: VITE_API_URL=/api
VITE_API_URL=https://nodo360-api.onrender.com/api
# Firebase/FCM se configura en nativo (google-services.json).
# No incluir google-services.json ni GoogleService-Info.plist en variables Vite.
