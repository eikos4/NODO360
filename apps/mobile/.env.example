# Desarrollo local: vacío usa proxy Vite (/api → localhost:3001)
# Android Emulator: http://10.0.2.2:3001/api (requiere permitir HTTP solo en desarrollo).
# VITE_API_URL=
# Firebase/FCM se configurará en el siguiente bloque nativo.
# No incluir google-services.json ni GoogleService-Info.plist en variables Vite.
